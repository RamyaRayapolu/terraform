# Ignore Terraform files
.terraform/
*.tfstate
*.tfstate.backup

# Ignore Terraform lock files (optional but useful)
.terraform.lock.hcl

# Ignore provider binaries (e.g., .exe files for Windows users)
*.exe
